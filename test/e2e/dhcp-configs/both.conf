# Scenario: dual-stack — IPv4 lease via DHCP + IPv6 SLAAC via RA.
# Both nftables families should end up populated.
interface=eth0
enable-ra
dhcp-range=10.99.0.50,10.99.0.99,255.255.255.0,12h
dhcp-range=fd99::50,fd99::99,slaac,64,12h
dhcp-authoritative
log-dhcp
